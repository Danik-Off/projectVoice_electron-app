# Стиль кода проекта ProjectVoice

## ✅ Настройка завершена

Весь код проекта приведен к единому стилю с помощью **Prettier** и **ESLint** со строгими правилами.

## 📦 Установленные пакеты

```json
{
  "@stylistic/eslint-plugin": "^2.x",
  "prettier": "^3.x",
  "eslint-config-prettier": "^10.x"
}
```

## 🎯 Основные правила

### Форматирование (Prettier + @stylistic)

| Правило | Значение | Уровень |
|---------|----------|---------|
| Отступы | 4 пробела | ERROR |
| Кавычки | Одинарные `'` | ERROR |
| Точка с запятой | Обязательна | ERROR |
| Длина строки | 120 символов | ERROR |
| Конец строки | LF (Unix) | ERROR |
| Trailing comma | Нет | ERROR |
| Пробелы в объектах | `{ key: value }` | ERROR |
| Пустые строки | Макс. 1 подряд | ERROR |

### TypeScript (строгие)

| Правило | Уровень | Описание |
|---------|---------|----------|
| `no-explicit-any` | ERROR | `any` запрещен |
| `no-unused-vars` | ERROR | Неиспользуемые переменные (кроме `_`) |
| `no-non-null-assertion` | ERROR | `!` запрещен |
| `consistent-type-imports` | ERROR | `import type` обязателен |
| `prefer-const` | ERROR | Обязательно `const` |
| `no-var` | ERROR | `var` запрещен |

### Качество кода

| Правило | Значение | Уровень |
|---------|----------|---------|
| `max-lines` | 500 строк | ERROR |
| `max-lines-per-function` | 100 строк | ERROR |
| `complexity` | 15 | ERROR |
| `max-depth` | 4 уровня | ERROR |
| `max-params` | 5 параметров | ERROR |
| `no-console` | Только warn/error | ERROR |
| `no-debugger` | Запрещен | ERROR |
| `no-alert` | Запрещен | ERROR |

### React

| Правило | Уровень | Описание |
|---------|---------|----------|
| `hooks/rules-of-hooks` | ERROR | Правила хуков |
| `hooks/exhaustive-deps` | ERROR | Все зависимости |
| `react-refresh` | ERROR | Только экспорт компонентов |

## 🚀 Команды

```bash
# Форматирование кода (Prettier)
npm run format

# Проверка форматирования
npm run format:check

# Проверка линтинга (ESLint)
npm run lint

# Автоисправление линтинга
npm run lint:fix

# Полная проверка
npm run format:check && npm run lint
```

## ⚠️ Важно

1. **Prettier** отвечает за форматирование (отступы, пробелы, кавычки)
2. **ESLint** отвечает за качество кода (логика, сложность, best practices)
3. **eslint-config-prettier** отключает конфликтующие правила ESLint
4. Сначала запускайте `npm run format`, потом `npm run lint:fix`

## 📝 Примеры

### ✅ Правильно

```typescript
import { useState, useEffect } from 'react';
import type { FC } from 'react';

interface UserProps {
    id: number;
    name: string;
}

export const UserCard: FC<UserProps> = ({ id, name }) => {
    const [loading, setLoading] = useState(false);

    useEffect(() => {
        setLoading(true);
    }, []);

    if (loading) {
        return <div>Loading...</div>;
    }

    return <div>{name}</div>;
};
```

### ❌ Неправильно

```typescript
import { useState, useEffect, FC } from 'react'; // type imports не разделены

const UserCard = ({ id, name }) => { // нет типов
    var loading = false; // var запрещен
    
    useEffect(() => {
        loading = true; // должен быть useState
    }); // нет зависимостей
    
    if (loading) return <div>Loading...</div>; // нет фигурных скобок
    else return <div>{name}</div>; // else после return
}
```

## 🔧 Настройки редактора

### VS Code (рекомендуется)

Рекомендуемые расширения (см. `.vscode/extensions.json`):
- ESLint
- Prettier - Code formatter
- EditorConfig for VS Code

### EditorConfig

Создан файл `.editorconfig` для унификации настроек во всех редакторах.

## 📊 Статистика

✅ **Отформатировано файлов**: 334  
✅ **Исправлено синтаксических ошибок**: 3  
✅ **Все файлы соответствуют стилю**: Да  
✅ **eslint-config-prettier**: Интегрирован  

## 🎓 Что изменилось

### Усилены правила (warn → error)

1. `@typescript-eslint/no-explicit-any` - `any` теперь ERROR
2. `no-alert` - полностью запрещен
3. `no-console` - только warn/error разрешены
4. `react-hooks/exhaustive-deps` - все зависимости обязательны
5. `react-refresh/only-export-components` - строгий контроль экспортов
6. `max-lines` - строгий лимит на размер файлов
7. `max-lines-per-function` - функции до 100 строк

### Добавлены новые правила

1. `complexity` - контроль цикломатической сложности (макс. 15)
2. `max-depth` - контроль вложенности (макс. 4)
3. `max-nested-callbacks` - контроль callback hell (макс. 3)
4. `max-params` - контроль количества параметров (макс. 5)
5. `consistent-type-imports` - разделение типов и значений
6. `member-delimiter-style` - стиль разделителей в интерфейсах

### Интеграция с Prettier

- Добавлен `eslint-config-prettier` для отключения конфликтующих правил
- Prettier управляет форматированием
- ESLint управляет качеством кода
- Нет конфликтов между инструментами

## 🔍 Временное отключение правил

Только в исключительных случаях с обязательным комментарием:

```typescript
// eslint-disable-next-line @typescript-eslint/no-explicit-any -- Legacy API требует any
const legacyData: any = oldApi.fetch();

// eslint-disable-next-line max-lines-per-function -- Сложная функция инициализации
function complexInit() {
    // ...
}
```

## 📚 Дополнительная документация

- `LINTING_RULES.md` - подробное описание всех правил линтинга
- `eslint.config.js` - конфигурация ESLint
- `.prettierrc.json` - конфигурация Prettier
- `.editorconfig` - настройки редактора

## ✨ Результат

Весь код проекта теперь:
- ✅ Единообразно отформатирован (Prettier)
- ✅ Соответствует строгим правилам качества (ESLint)
- ✅ Без конфликтов между инструментами (eslint-config-prettier)
- ✅ Автоматически проверяется при сохранении (в VS Code)
- ✅ Легко поддерживается и расширяется
