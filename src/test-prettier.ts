// Этот файл отформатирован Prettier, но может не соответствовать правилам @stylistic
// Намеренно нарушаем правила @stylistic для проверки конфликтов
const test = { a: 1, b: 2 }; // Нарушение: нет пробелов
const arr = [1, 2, 3]; // Нарушение: нет пробелов
function testFunc(a: number, b: number) {
    return a + b;
} // Нарушение: нет пробелов, нет точки с запятой
const arrow = (x: number) => x * 2; // Нарушение: нет пробелов, нет точки с запятой
