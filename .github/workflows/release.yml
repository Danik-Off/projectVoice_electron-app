name: Build and Release

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'package.json'
  workflow_dispatch:

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      version-changed: ${{ steps.check.outputs.changed }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Get current version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $VERSION"

      - name: Check if version changed
        id: check
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          
          # Пытаемся получить предыдущую версию из предыдущего коммита
          PREVIOUS_VERSION=""
          if git show HEAD~1:package.json > /dev/null 2>&1; then
            PREVIOUS_VERSION=$(git show HEAD~1:package.json | node -p "JSON.parse(require('fs').readFileSync(0, 'utf-8')).version" 2>/dev/null || echo "")
          fi
          
          # Если не удалось получить из предыдущего коммита, проверяем последний тег
          if [ -z "$PREVIOUS_VERSION" ]; then
            LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
            if [ -n "$LAST_TAG" ]; then
              PREVIOUS_VERSION=$(echo "$LAST_TAG" | sed 's/^v//')
            fi
          fi
          
          if [ "$CURRENT_VERSION" != "$PREVIOUS_VERSION" ] && [ -n "$PREVIOUS_VERSION" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ Version changed from $PREVIOUS_VERSION to $CURRENT_VERSION"
          elif [ -z "$PREVIOUS_VERSION" ]; then
            # Если это первый релиз, все равно создаем релиз
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ First release detected: $CURRENT_VERSION"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Version not changed: $CURRENT_VERSION"
          fi

  build-linux:
    needs: check-version
    if: needs.check-version.outputs.version-changed == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    continue-on-error: true
    outputs:
      success: ${{ steps.set-output.outputs.success }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Clean install dependencies
        run: |
          echo "🧹 Cleaning node_modules and package-lock.json..."
          rm -rf node_modules package-lock.json
          
          echo "📦 Installing dependencies..."
          npm install --legacy-peer-deps
          
          echo "🔧 Ensuring rollup native dependencies are installed..."
          # Устанавливаем нативные зависимости rollup для Linux
          npm install @rollup/rollup-linux-x64-gnu --save-optional --force || echo "⚠️ Rollup native dependency install failed, but continuing..."
          
          # Проверяем, что rollup работает
          echo "🧪 Testing rollup..."
          npx rollup --version || echo "⚠️ Rollup test failed"
          
          echo "✅ Dependencies installed"

      - name: Build application for Linux
        id: build
        run: |
          echo "🔨 Starting Linux build..."
          npm run build:linux || BUILD_EXIT_CODE=$?
          echo "Build exit code: ${BUILD_EXIT_CODE:-0}"
          
          if [ "${BUILD_EXIT_CODE:-0}" -eq 0 ]; then
            echo "✅ Linux build successful"
          else
            echo "❌ Linux build failed with exit code ${BUILD_EXIT_CODE}"
          fi

      - name: Set build output
        id: set-output
        if: always()
        run: |
          if [ -d "build" ] && [ "$(ls -A build 2>/dev/null)" ]; then
            echo "success=true" >> $GITHUB_OUTPUT
            echo "✅ Build artifacts found"
          else
            echo "success=false" >> $GITHUB_OUTPUT
            echo "❌ No build artifacts found"
          fi

      - name: Upload build artifacts (Linux)
        if: steps.set-output.outputs.success == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: build-linux-${{ needs.check-version.outputs.version }}
          path: build/*
          retention-days: 30
          if-no-files-found: error

  build-windows:
    needs: check-version
    if: needs.check-version.outputs.version-changed == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: windows-latest
    continue-on-error: true
    outputs:
      success: ${{ steps.set-output.outputs.success }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Clean install dependencies
        shell: pwsh
        run: |
          Write-Host "🧹 Cleaning node_modules and package-lock.json..."
          Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
          Remove-Item -Force package-lock.json -ErrorAction SilentlyContinue
          
          Write-Host "📦 Installing dependencies..."
          npm install --legacy-peer-deps
          
          Write-Host "🧪 Testing rollup..."
          npx rollup --version | Out-Null
          if ($LASTEXITCODE -ne 0) {
            Write-Host "⚠️ Rollup test failed, but continuing..."
          }
          
          Write-Host "✅ Dependencies installed"

      - name: Build application for Windows
        id: build
        shell: pwsh
        run: |
          Write-Host "🔨 Starting Windows build..."
          $ErrorActionPreference = "Continue"
          npm run build:win
          $buildExitCode = $LASTEXITCODE
          Write-Host "Build exit code: $buildExitCode"
          
          if ($buildExitCode -eq 0) {
            Write-Host "✅ Windows build successful"
          } else {
            Write-Host "❌ Windows build failed with exit code $buildExitCode"
          }

      - name: Set build output
        id: set-output
        if: always()
        shell: pwsh
        run: |
          if (Test-Path "build" -PathType Container) {
            $files = Get-ChildItem -Path "build" -Recurse -File -ErrorAction SilentlyContinue
            if ($files.Count -gt 0) {
              "success=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
              Write-Host "✅ Build artifacts found: $($files.Count) file(s)"
            } else {
              "success=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
              Write-Host "❌ No build artifacts found"
            }
          } else {
            "success=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
            Write-Host "❌ Build directory not found"
          }

      - name: Upload build artifacts (Windows)
        if: steps.set-output.outputs.success == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: build-win-${{ needs.check-version.outputs.version }}
          path: build/*
          retention-days: 30
          if-no-files-found: error

  create-release:
    needs: [check-version, build-linux, build-windows]
    if: |
      (needs.check-version.outputs.version-changed == 'true' || github.event_name == 'workflow_dispatch') &&
      (needs.build-linux.outputs.success == 'true' || needs.build-windows.outputs.success == 'true')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Получаем всю историю для генерации changelog

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Generate release notes
        id: release-notes
        run: |
          node scripts/generate-release-notes.js > release-notes.md || echo "# Release v${{ needs.check-version.outputs.version }}" > release-notes.md
          echo "notes<<EOF" >> $GITHUB_OUTPUT
          cat release-notes.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: build-*
          merge-multiple: false
          fail-on-error: false

      - name: Organize artifacts by platform
        run: |
          mkdir -p release-files
          
          echo "📦 Searching for artifacts..."
          echo "Available artifact directories:"
          find artifacts -type d -maxdepth 2 2>/dev/null | head -20 || echo "No artifacts directory found"
          
          # Копируем файлы из артефактов Linux
          LINUX_DIR="artifacts/build-linux-${{ needs.check-version.outputs.version }}"
          if [ -d "$LINUX_DIR" ]; then
            echo "📦 Copying Linux artifacts from $LINUX_DIR..."
            find "$LINUX_DIR" -type f -exec cp {} release-files/ \;
            echo "✅ Linux artifacts copied"
          else
            echo "⚠️ Linux artifacts not found at $LINUX_DIR"
          fi
          
          # Копируем файлы из артефактов Windows
          WIN_DIR="artifacts/build-win-${{ needs.check-version.outputs.version }}"
          if [ -d "$WIN_DIR" ]; then
            echo "📦 Copying Windows artifacts from $WIN_DIR..."
            find "$WIN_DIR" -type f -exec cp {} release-files/ \;
            echo "✅ Windows artifacts copied"
          else
            echo "⚠️ Windows artifacts not found at $WIN_DIR"
          fi
          
          echo ""
          echo "📋 Release files:"
          ls -lh release-files/ 2>/dev/null || echo "No files found"
          
          # Проверяем, что есть хотя бы один файл
          FILE_COUNT=$(ls -1 release-files 2>/dev/null | wc -l)
          if [ "$FILE_COUNT" -eq 0 ]; then
            echo "❌ Error: No release files found!"
            echo "Available artifacts structure:"
            find artifacts -type f 2>/dev/null | head -10 || echo "No artifacts found"
            exit 1
          fi
          
          echo "✅ Found $FILE_COUNT file(s) for release"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.check-version.outputs.version }}
          name: 🚀 Release v${{ needs.check-version.outputs.version }}
          body: ${{ steps.release-notes.outputs.notes }}
          files: |
            release-files/**/*
          draft: false
          prerelease: false
          generate_release_notes: false
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify:
    needs: [check-version, build-linux, build-windows, create-release]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Build Status Summary
        run: |
          echo "## 📊 Build Status Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Platform | Status | Details |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------|---------|" >> $GITHUB_STEP_SUMMARY
          
          LINUX_RESULT="${{ needs.build-linux.result }}"
          LINUX_SUCCESS="${{ needs.build-linux.outputs.success }}"
          if [ "$LINUX_RESULT" == "success" ] && [ "$LINUX_SUCCESS" == "true" ]; then
            echo "| Linux | ✅ Success | Build completed and artifacts uploaded |" >> $GITHUB_STEP_SUMMARY
          elif [ "$LINUX_RESULT" == "success" ]; then
            echo "| Linux | ⚠️ Partial | Build completed but no artifacts found |" >> $GITHUB_STEP_SUMMARY
          elif [ "$LINUX_RESULT" == "failure" ]; then
            echo "| Linux | ❌ Failed | Build failed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Linux | ⏭️ Skipped | Not executed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          WIN_RESULT="${{ needs.build-windows.result }}"
          WIN_SUCCESS="${{ needs.build-windows.outputs.success }}"
          if [ "$WIN_RESULT" == "success" ] && [ "$WIN_SUCCESS" == "true" ]; then
            echo "| Windows | ✅ Success | Build completed and artifacts uploaded |" >> $GITHUB_STEP_SUMMARY
          elif [ "$WIN_RESULT" == "success" ]; then
            echo "| Windows | ⚠️ Partial | Build completed but no artifacts found |" >> $GITHUB_STEP_SUMMARY
          elif [ "$WIN_RESULT" == "failure" ]; then
            echo "| Windows | ❌ Failed | Build failed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Windows | ⏭️ Skipped | Not executed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          RELEASE_RESULT="${{ needs.create-release.result }}"
          if [ "$RELEASE_RESULT" == "success" ]; then
            echo "| Release | ✅ Created | Release v${{ needs.check-version.outputs.version }} created successfully |" >> $GITHUB_STEP_SUMMARY
          elif [ "$RELEASE_RESULT" == "failure" ]; then
            echo "| Release | ❌ Failed | Failed to create release |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Release | ⏭️ Skipped | Not executed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ needs.check-version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Version Changed:** ${{ needs.check-version.outputs.version-changed }}" >> $GITHUB_STEP_SUMMARY
